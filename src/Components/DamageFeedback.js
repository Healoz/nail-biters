export default function DamageFeedback(props) {

    // generate a random number between 1 and 4
    // play animation of number appearing and disappearing in a random direction

    return (
        <h3 className="damage-number">40</h3>
    )
}